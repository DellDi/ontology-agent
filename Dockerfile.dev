FROM node:24.14.0-bookworm

WORKDIR /workspace

ENV PNPM_HOME=/pnpm
ENV PATH=$PNPM_HOME:$PATH
ENV APP_PORT=3000

RUN corepack enable

CMD ["bash", "-lc", "./scripts/docker/ensure-node-modules.sh && pnpm dev --hostname 0.0.0.0 --port $APP_PORT"]
